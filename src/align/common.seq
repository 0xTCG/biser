# hash function for sequences
def hash_dna(c: seq) -> int:
    k = int((c.__int__()))
    return 0 if k == 4 else k


def align_dna(c):
    return int(seq(c))


def rev_dna(c):
    return str(~seq(c))


def pct(p: float, tot: float):
    return 100 * p / tot
